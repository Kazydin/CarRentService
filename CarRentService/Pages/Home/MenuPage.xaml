<?xml version="1.0" encoding="utf-8"?>

<abstract:BasePage
    x:Class="CarRentService.Pages.Home.MenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:abstract="using:CarRentService.Common.Abstract"
    xmlns:home="using:CarRentService.Pages.Home"
    xmlns:common="using:CarRentService.Common"
    d:DataContext="{d:DesignInstance Type=home:MenuViewModel}"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="HomePage_OnLoaded">

    <NavigationView
        x:Name="Navi"
        IsSettingsVisible="False"
        IsBackEnabled="True"
        BackRequested="NavigationViewControl_BackRequested"
        IsBackButtonVisible="{Binding CanGoBack, Mode=OneWay}"
        SelectionChanged="Navi_OnSelectionChanged">
        <NavigationView.MenuItems>
            <NavigationViewItemHeader
                Content="Обслуживание" />

            <NavigationViewItem
                Content="Клиенты"
                Tag="{x:Bind common:PageTypeEnum.Clients, Converter={StaticResource EnumToStringConverter}}">
                <NavigationViewItem.Icon>
                    <BitmapIcon
                        UriSource="ms-appx:///Assets/clients.png" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem
                Content="Автомобили"
                Tag="{x:Bind common:PageTypeEnum.Cars, Converter={StaticResource EnumToStringConverter}}">
                <NavigationViewItem.Icon>
                    <BitmapIcon
                        UriSource="ms-appx:///Assets/car.png" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <NavigationViewItemHeader
                Content="Сделки" />

            <NavigationViewItem
                Content="Договора"
                Tag="{x:Bind common:PageTypeEnum.Contracts, Converter={StaticResource EnumToStringConverter}}">
                <NavigationViewItem.Icon>
                    <BitmapIcon
                        UriSource="ms-appx:///Assets/contract.png" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem
                Content="Платежи"
                Tag="{x:Bind common:PageTypeEnum.Payments, Converter={StaticResource EnumToStringConverter}}">
                <NavigationViewItem.Icon>
                    <BitmapIcon
                        UriSource="ms-appx:///Assets/payment.png" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem
                Content="Страховки"
                Tag="{x:Bind common:PageTypeEnum.Insurances, Converter={StaticResource EnumToStringConverter}}">
                <NavigationViewItem.Icon>
                    <BitmapIcon
                        UriSource="ms-appx:///Assets/insurance.png" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <NavigationViewItemHeader
                Content="Администрирование" />

            <NavigationViewItem
                Content="Филиалы"
                Tag="{x:Bind common:PageTypeEnum.Branches, Converter={StaticResource EnumToStringConverter}}">
                <NavigationViewItem.Icon>
                    <BitmapIcon
                        UriSource="ms-appx:///Assets/branch.png" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem
                Content="Управление персоналом"
                Tag="{x:Bind common:PageTypeEnum.ManageAdmins, Converter={StaticResource EnumToStringConverter}}"
                Icon="Contact" />

        </NavigationView.MenuItems>
        <NavigationView.FooterMenuItems>
            <NavigationViewItem
                Content="Иванов Иван Иванович"
                Tag="{x:Bind common:PageTypeEnum.Account, Converter={StaticResource EnumToStringConverter}}">
                <NavigationViewItem.Icon>
                    <BitmapIcon
                        UriSource="ms-appx:///Assets/user.png" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem
                Content="Выйти"
                Tag="{x:Bind common:PageTypeEnum.Exit, Converter={StaticResource EnumToStringConverter}}">
                <NavigationViewItem.Icon>
                    <BitmapIcon
                        UriSource="ms-appx:///Assets/switch.png" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.FooterMenuItems>

        <Frame
            x:Name="ContentFrame"/>
    </NavigationView>
</abstract:BasePage>